{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/assets/*.{js,css,png,jpg,jpeg,gif,svg}", "/api/*"]
  },
  "routes": [
    {
      "route": "/api/*",
      "allowedMethods": ["GET", "POST", "PUT", "PATCH", "DELETE"],
      "rewrite": "https://bolaoonline-docker.azurewebsites.net/api/*"
    }
  ]
}